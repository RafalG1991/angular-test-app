import { CountMorePipe } from './count-more.pipe';

describe('CountMorePipe', () => {
  it('create an instance', () => {
    const pipe = new CountMorePipe();
    expect(pipe).toBeTruthy();
  });
});
